{
  "name": "theoaktells.com",
  "version": "1.0.0",
  "description": "website for sculptures",
  "scripts": {
    "build:dev": "nodemon scripts/build.js",
    "run:dev": "serve build/",
    "build": "node scripts/build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "author": "jlvn",
  "license": "ISC",
  "dependencies": {
    "date-fns": "^4.1.0",
    "handlebars": "^4.7.8",
    "nodemon": "^3.1.10",
    "serve": "^14.2.4",
    "sharp": "^0.34.2"
  },
  "nodemonConfig": {
    "ext": "js, json, css, handlebars",
    "ignore": [
      "build/**"
    ]
  }
}
