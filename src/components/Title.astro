---
interface Props {
    title: string
    class?: string
}

const {title, class: className} = Astro.props
---

<h1 class:list={['text-5xl',
    'tracking-tighter',
    'text-accent',
    'font-bold',
    'font-serif',
    className]}>{title}</h1>
